{
  "name": "candid-parser-wasm",
  "version": "1.0.0",
  "description": "This WASM library provides bindings to (and helper functions for) `candid_parser` crate.",
  "author": "Artyom Titov",
  "license": "MIT",
  "repository": {
    "url": "https://github.com/justblender/candid-parser-wasm"
  },
  "scripts": {
    "build": "rimraf pkg && wasm-pack build --target bundler",
    "publish": "npm run build && wasm-pack publish"
  },
  "devDependencies": {
    "rimraf": "^5.0.5"
  }
}
